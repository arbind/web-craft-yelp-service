describe 'WebCraftYelpService', ->
  service = undefined

  before (done)->
    # service.clearCache()
    done()

  after (done)->
    # service.clearCache()
    done()
  it 'placeholder', (done)->
    expect(true).to.equal false
    done()
